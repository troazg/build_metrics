<div class="card-deck mb-3 text-center">
  {{#each builds}}
    <div class="card">
      <h5 class="card-title">
        <a href="/builds/{{_id}}">{{name}}</a>
      </h5>
      <p class="card-text">
        Info about the build.
      </p>
    </div>
  {{/each}}
</div>
